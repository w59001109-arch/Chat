# Port for the web server (default: 3000)
PORT=3000

# Path to claude binary (default: claude, assumes it's in PATH)
# If claude is not in PATH, set the full path:
# CLAUDE_BIN=/usr/local/bin/claude
CLAUDE_BIN=claude
